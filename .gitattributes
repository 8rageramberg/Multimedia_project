# Auto detect text files and perform LF normalization
* text=auto

# Add csv files to lfs system
feature_db/Pose_estimator_features.csv filter=lfs diff=lfs merge=lfs -text
feature_db/SIFT_descriptor_detector_features.csv filter=lfs diff=lfs merge=lfs -text
